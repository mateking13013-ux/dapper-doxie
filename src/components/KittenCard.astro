---
// KittenCard Component - Available Kittens Page (same layout as original PuppyCard)
interface Props {
  kittens: {
    name: string;
    breed: string;
    breedName: string;
    image: string;
    sex: string;
    age: string;
    status: string;
    price: number;
    description: string;
  }[];
}

const { kittens } = Astro.props;
---

<section class="puppies-section">
  <div class="container">
    {kittens.map((kitten, kittenIndex) => (
      <div class="puppy-card">
        <div class="puppy-gallery">
          <div class="main-image">
            <img id={`main-img-${kittenIndex}`} src={kitten.image} alt={kitten.name} />
          </div>
        </div>

        <div class="puppy-details">
          <div class="details-header">
            <div class="name-section">
              <h2>{kitten.name}</h2>
              <p class="subtitle">ID: CC{String(kittenIndex + 1).padStart(3, '0')}</p>
            </div>
            <span class={`status-badge ${kitten.status.toLowerCase()}`}>{kitten.status}</span>
          </div>

          <div class="price-banner">
            <span class="price-amount">{kitten.price.toLocaleString('es-ES')} &euro;</span>
            <span class="price-text">Incluye Documentación y Kit de Bienvenida</span>
          </div>

          <div class="stats-row">
            <div class="stat-item">
              <div class="stat-icon">
                <svg viewBox="0 0 24 24" fill="currentColor">
                  <circle cx="4.5" cy="9.5" r="2.5"/>
                  <circle cx="9" cy="5.5" r="2.5"/>
                  <circle cx="15" cy="5.5" r="2.5"/>
                  <circle cx="19.5" cy="9.5" r="2.5"/>
                  <path d="M17.34 14.86c-.87-1.02-1.6-1.89-2.48-2.91-.46-.54-1.05-1.08-1.75-1.32-.11-.04-.22-.07-.33-.09-.25-.04-.52-.04-.78-.04s-.53 0-.79.05c-.11.02-.22.05-.33.09-.7.24-1.28.78-1.75 1.32-.87 1.02-1.6 1.89-2.48 2.91-1.31 1.31-2.92 2.76-2.62 4.79.29 1.02 1.02 2.03 2.33 2.32.73.15 3.06-.44 5.54-.44h.18c2.48 0 4.81.58 5.54.44 1.31-.29 2.04-1.31 2.33-2.32.31-2.04-1.3-3.49-2.61-4.8z"/>
                </svg>
              </div>
              <span class="stat-value">{kitten.sex}</span>
              <span class="stat-label">Sexo</span>
            </div>
            <div class="stat-item">
              <div class="stat-icon">
                <svg viewBox="0 0 24 24" fill="currentColor">
                  <path d="M19 3h-1V1h-2v2H8V1H6v2H5c-1.11 0-1.99.9-1.99 2L3 19c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V5c0-1.1-.9-2-2-2zm0 16H5V8h14v11zM9 10H7v2h2v-2zm4 0h-2v2h2v-2zm4 0h-2v2h2v-2zm-8 4H7v2h2v-2zm4 0h-2v2h2v-2zm4 0h-2v2h2v-2z"/>
                </svg>
              </div>
              <span class="stat-value">{kitten.age}</span>
              <span class="stat-label">Edad</span>
            </div>
            <div class="stat-item">
              <div class="stat-icon">
                <svg viewBox="0 0 24 24" fill="currentColor">
                  <circle cx="4.5" cy="9.5" r="2.5"/>
                  <circle cx="9" cy="5.5" r="2.5"/>
                  <circle cx="15" cy="5.5" r="2.5"/>
                  <circle cx="19.5" cy="9.5" r="2.5"/>
                  <path d="M17.34 14.86c-.87-1.02-1.6-1.89-2.48-2.91-.46-.54-1.05-1.08-1.75-1.32-.11-.04-.22-.07-.33-.09-.25-.04-.52-.04-.78-.04s-.53 0-.79.05c-.11.02-.22.05-.33.09-.7.24-1.28.78-1.75 1.32-.87 1.02-1.6 1.89-2.48 2.91-1.31 1.31-2.92 2.76-2.62 4.79.29 1.02 1.02 2.03 2.33 2.32.73.15 3.06-.44 5.54-.44h.18c2.48 0 4.81.58 5.54.44 1.31-.29 2.04-1.31 2.33-2.32.31-2.04-1.3-3.49-2.61-4.8z"/>
                </svg>
              </div>
              <span class="stat-value">{kitten.breedName}</span>
              <span class="stat-label">Raza</span>
            </div>
            <div class="stat-item">
              <div class="stat-icon">
                <svg viewBox="0 0 24 24" fill="currentColor">
                  <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                </svg>
              </div>
              <span class="stat-value">{kitten.status}</span>
              <span class="stat-label">Estado</span>
            </div>
          </div>

          <div class="info-row">
            <div class="info-item">
              <span class="info-label">ID Gatito:</span>
              <span class="info-value">CC{String(kittenIndex + 1).padStart(3, '0')}</span>
            </div>
            <div class="info-item">
              <span class="info-label">Registro:</span>
              <span class="info-value">Sí, Pedigrí FIFE</span>
            </div>
          </div>

          <p class="description">{kitten.description}</p>

          <div class="comes-with-card">
            <p class="comes-with-title">Cada Gatito Incluye:</p>
            <ul class="comes-with-list">
              <li>Garantía de salud de 2 años</li>
              <li>Vacunas al día</li>
              <li>Desparasitación completa</li>
              <li>Microchip y documentación</li>
              <li>Kit de bienvenida</li>
            </ul>
          </div>

          <div class="buttons-row">
            <a href="/contacto" class="btn btn-primary">Contactar</a>
            <button type="button" class="btn btn-outline" onclick={`openContactPopup('${kitten.name}', '${kitten.image}', '${kitten.breedName} · ${kitten.sex} · ${kitten.age}', ${kitten.price})`}>Adoptar</button>
          </div>
        </div>
      </div>
    ))}
  </div>
</section>

<style>
  .puppies-section {
    padding: 40px 20px;
    background: #F0F4F8;
  }

  .puppies-section .container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .puppy-card {
    display: grid;
    grid-template-columns: 1fr 1fr;
    background: #fff;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    margin-bottom: 30px;
  }

  .puppy-gallery {
    position: relative;
    min-height: 450px;
  }

  .main-image {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
  }

  .main-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
    display: block;
  }

  .puppy-details {
    padding: 20px 25px;
    display: flex;
    flex-direction: column;
  }

  .details-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 15px;
  }

  .name-section h2 {
    font-size: 1.5rem;
    color: #333;
    margin: 0 0 3px;
  }

  .subtitle {
    font-size: 0.8rem;
    color: #888;
    margin: 0;
  }

  .status-badge {
    padding: 6px 12px;
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    border-radius: 4px;
    letter-spacing: 0.5px;
  }

  .status-badge.disponible {
    background: #1B2A4A;
    color: #fff;
  }

  .status-badge.reservado {
    background: #E8A94A;
    color: #0F1B33;
  }

  .price-banner {
    background: linear-gradient(135deg, #1B2A4A 0%, #2E8B8B 100%);
    color: #fff;
    padding: 12px 16px;
    border-radius: 8px;
    margin-bottom: 15px;
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
  }

  .price-amount {
    font-size: 1.5rem;
    font-weight: 800;
    color: #fff;
  }

  .price-text {
    font-size: 0.9rem;
    font-weight: 600;
    opacity: 0.95;
  }

  .stats-row {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 10px;
    padding: 12px 0;
    border-top: 1px solid #E2E8F0;
    border-bottom: 1px solid #E2E8F0;
    margin-bottom: 12px;
  }

  .stat-item {
    text-align: center;
  }

  .stat-icon {
    width: 28px;
    height: 28px;
    margin: 0 auto 5px;
    color: #1B2A4A;
  }

  .stat-icon svg {
    width: 100%;
    height: 100%;
  }

  .stat-value {
    display: block;
    font-weight: 700;
    color: #333;
    font-size: 0.85rem;
    margin-bottom: 1px;
  }

  .stat-label {
    font-size: 0.65rem;
    color: #888;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .info-row {
    display: flex;
    gap: 20px;
    margin-bottom: 12px;
    flex-wrap: wrap;
  }

  .info-item {
    display: flex;
    gap: 6px;
  }

  .info-label {
    color: #888;
    font-size: 0.8rem;
  }

  .info-value {
    color: #333;
    font-weight: 600;
    font-size: 0.8rem;
  }

  .description {
    font-size: 0.85rem;
    color: #555;
    line-height: 1.6;
    margin-bottom: 15px;
    flex-grow: 1;
  }

  .comes-with-card {
    background: #F0F4F8;
    border: 1px solid #2E8B8B;
    border-radius: 8px;
    padding: 10px 14px;
    margin-bottom: 15px;
  }

  .comes-with-card .comes-with-title {
    margin: 0 0 8px;
    font-size: 0.85rem;
    color: #0F1B33;
    font-weight: 700;
    line-height: 1.5;
  }

  .comes-with-card .comes-with-list {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .comes-with-card .comes-with-list li {
    font-size: 0.8rem;
    color: #0F1B33;
    padding: 2px 0;
    padding-left: 18px;
    position: relative;
  }

  .comes-with-card .comes-with-list li::before {
    content: "\2713";
    position: absolute;
    left: 0;
    color: #2E8B8B;
    font-weight: 700;
  }

  .buttons-row {
    display: flex;
    gap: 10px;
  }

  .buttons-row .btn {
    flex: 1;
    text-align: center;
    padding: 10px 15px;
    font-size: 0.8rem;
    font-weight: 700;
    border-radius: 5px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    transition: all 0.3s ease;
    cursor: pointer;
    text-decoration: none;
  }

  .btn-primary {
    background: #1B2A4A;
    color: #fff;
    border: 2px solid #1B2A4A;
  }

  .btn-primary:hover {
    background: #0F1B33;
    border-color: #0F1B33;
    color: #fff;
  }

  .btn-outline {
    background: transparent;
    color: #1B2A4A;
    border: 2px solid #1B2A4A;
  }

  .btn-outline:hover {
    background: #1B2A4A;
    color: #fff;
  }

  @media (max-width: 900px) {
    .puppy-card {
      grid-template-columns: 1fr;
    }

    .puppy-gallery {
      min-height: auto;
      display: flex;
      flex-direction: column;
    }

    .main-image {
      position: relative;
      height: 300px;
    }

    .buttons-row {
      flex-direction: column;
      gap: 8px;
    }
  }

  @media (max-width: 600px) {
    .puppies-section {
      padding: 20px 10px;
    }

    .main-image {
      height: 380px;
    }

    .puppy-details {
      padding: 15px;
    }

    .details-header {
      margin-bottom: 10px;
    }

    .price-banner {
      padding: 10px 12px;
      margin-bottom: 12px;
      flex-direction: column;
      align-items: flex-start;
      gap: 4px;
      text-align: center;
    }

    .price-amount {
      font-size: 1.3rem;
      width: 100%;
    }

    .price-text {
      font-size: 0.8rem;
      width: 100%;
    }

    .name-section h2 {
      font-size: 1.25rem;
    }

    .subtitle {
      font-size: 0.75rem;
    }

    .status-badge {
      padding: 5px 10px;
      font-size: 0.65rem;
    }

    .stats-row {
      padding: 10px 0;
      margin-bottom: 10px;
      gap: 5px;
    }

    .stat-icon {
      width: 22px;
      height: 22px;
      margin-bottom: 3px;
    }

    .stat-value {
      font-size: 0.7rem;
    }

    .stat-label {
      font-size: 0.55rem;
    }

    .info-row {
      gap: 12px;
      margin-bottom: 10px;
    }

    .info-label,
    .info-value {
      font-size: 0.75rem;
    }

    .description {
      font-size: 0.8rem;
      line-height: 1.5;
      margin-bottom: 12px;
    }

    .buttons-row .btn {
      padding: 10px 12px;
      font-size: 0.75rem;
    }
  }
</style>
